let greeting = (name, greeting = "HI") => {
  console.log(`${greeting} ${name}`);
};

greeting("Meet", "Heelo! Good Morning!!");
greeting("Meet");

/*We can set default as anything(Ex. array, string, number)
 We can set multiple Default Parameter!
*/
